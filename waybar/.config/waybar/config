{
    "layer": "top",
    "position": "top",
    "modules-left": ["custom/nixos", "hyprland/window"],
    "modules-center": ["hyprland/workspaces"],
    "modules-right": ["temperature#cpu", "temperature#gpu", "clock"],
    "margin-right": 8,
    "margin-left": 8,
    "margin-top": 4,
    "margin-bottom": 0,
    "height": 32,
    "spacing": 4,

    "custom/nixos": {
        "format": "<img>",
        "tooltip-format": "NixOS",
        "on-click": "rofi -show drun"
    },

    "hyprland/window": {
        "format": "{class}",
        "max-length": 20,
        "min-length": 10,
    },

    "hyprland/workspaces": {
        "format": "{name}",
        "on-click": "activate"
    },

    "clock": {
        "format": "🕒 {:%Y-%m-%d %H:%M}"
    },

    "temperature#cpu": {
      "hwmon-path": "/sys/class/hwmon/hwmon3/temp1_input",
      "critical-threshold": 80,
      "format-critical": "🔥 CPU {temperatureC}°C",
      "format": "🌡️ CPU {temperatureC}°C"
  },
  "temperature#gpu": {
      "hwmon-path": "/sys/class/hwmon/hwmon4/temp1_input", 
      "critical-threshold": 85,
      "format-critical": "🔥 GPU {temperatureC}°C",
      "format": "🎮 GPU {temperatureC}°C"
  }
}

